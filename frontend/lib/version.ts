// App version + a one-line summary of the most recent changes, surfaced next to the main
// header. Bump APP_VERSION (SemVer) and refresh APP_VERSION_SUMMARY on each notable release;
// keep package.json's "version" in step.
export const APP_VERSION = '0.2.0'

// Brief, present-tense summary of what shipped in APP_VERSION. Shown italicized by the header.
export const APP_VERSION_SUMMARY =
  'Added Recommended Action triage; reorganized nav into Ordering and trimmed unused tabs.'
